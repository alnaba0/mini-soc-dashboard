Mini SOC Dashboard

Run with:
1. pip install -r requirements.txt
2. python app.py

New additions in this version:
- black and gray theme
- alert detail expansion
- export logs and alerts to CSV
- log search and severity filter
- local sample file upload preview for JSON, CSV, and TXT
